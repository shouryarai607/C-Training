#include<stdio.h>

    int prime(n){
        if (n){
        printf("It is prime no.");}
        else { printf("It is prime no.");}

    }

    int main(){
        int n;
        printf("Enter no. to check");
        scanf("%d",&n);
        prime(n);
    }
